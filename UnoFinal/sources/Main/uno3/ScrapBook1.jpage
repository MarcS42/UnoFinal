Syntax error on token "(", ; expectedSyntax error on token "(", ; expectedpublic int stringMatch(String a, String b) {  int count=0,index1;  String findMe = "";  for(int i = 0; i < a.length()-1;i++){    findMe.equals(a.substring(i,i+2));    if(b.contains(findMe)){      index1 = a.indexOf(findMe);      if(b.indexOf(findMe) == index1){        count++;      }    }  }  return count;}public static void main(String[] args){    String a = "xxcaazz";    String b = "xxbaaz";        int kount = stringMatch(a,b);    System.out.println("Kount=: " + kount);}